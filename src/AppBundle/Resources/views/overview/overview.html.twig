<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title> Overview</title>
</head>
<body>
    <h1>Users Overview</h1>
{% if users|length == 0 %}
     <h3> no users found</h3>
    {% endif %}
    <ul>
        {% for user in users %}
            <li>{{ user.firstName }} : {{ user.lastName }} </li>

        {% endfor %}
    </ul>
</body>
</html>